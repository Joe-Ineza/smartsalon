package com.SSWebApp.SmartSallonWebApp.repository;

import com.SSWebApp.SmartSallonWebApp.models.SalonServ;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalonServRepository extends JpaRepository<SalonServ, Long> {
}
